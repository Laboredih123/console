{
	"version": "0.2.0",
	"configurations": [
		{
		"type": "byond",
		"request": "launch",
		"name": "Launch DreamSeeker",
		"preLaunchTask": "dm: build - console.dme",
		"dmb": "${workspaceFolder}/${command:CurrentDMB}"
		}
	]
}
